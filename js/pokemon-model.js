class Pokemon {
  number;           // = id da Pokédex
  name;
  type;
  types = [];

  // About
  height;           // em decímetros (API)
  weight;           // em hectogramas (API)
  abilities = [];

  // imagem
  photo;
}
